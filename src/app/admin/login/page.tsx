
// Login functionality has been removed.
// You can access the admin panel directly if needed.

export default function AdminLoginPagePlaceholder() {
  return (
    <div className="flex items-center justify-center min-h-[calc(100vh-200px)] py-12">
      <div className="text-center">
        <h1 className="text-2xl font-semibold mb-4">Admin Login</h1>
        <p className="text-muted-foreground">
          Login functionality has been removed from this application.
        </p>
        <p className="text-muted-foreground mt-2">
          Admin sections are now directly accessible if they exist.
        </p>
      </div>
    </div>
  );
}
